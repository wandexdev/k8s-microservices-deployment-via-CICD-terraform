  apiVersion: networking.k8s.io/v1
  kind: Ingress
  metadata:
    name: sock-shop-ingress
    namespace: sock-shop
    #annotations:
    #  nginx.ingress.kubernetes.io/rewritw-target: /
  spec:
    ingressClassName: nginx
    rules:
       -  host: sock-shop.wandexdev.me
          http:
            paths:
            - backend:
                service:
                  name: front-end
                  port:
                    number: 80
              path: /
              pathType: Prefix